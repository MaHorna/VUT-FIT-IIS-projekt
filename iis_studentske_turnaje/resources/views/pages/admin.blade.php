<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{config('app.name', 'IIS projekt studentske turnaje')}}</title>
    </head>
    <body>
	<p>admin page</p>
	<p>list of users - everything is textfiled with values, one button to send changes in that row (user) , one button to delete user ?</p>
	<p>list of tournaments to "sch·liù" - rows, one row - one team name, acts as link to that team page. one button to allow tournament , one button to delete/disallow tournament</p>
    </body>
</html>
