<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{config('app.name', 'IIS projekt studentske turnaje')}}</title>
    </head>
    <body>
	<p>list of all tournaments</p>
	<p>search function ? (TF + button)</p>
	<p>every row one tournament, links to tournament page + some info about it</p>
    </body>
</html>
